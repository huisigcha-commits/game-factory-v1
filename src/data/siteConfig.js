// Final brand name and domain are a user decision Gate. Do not replace the placeholder until then.
export const siteConfig = Object.freeze({
  siteName: 'GAME FACTORY',
  siteUrl: 'https://YOUR-DOMAIN.example',
  locale: 'ko-KR',
});

