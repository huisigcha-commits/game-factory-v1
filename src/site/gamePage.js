import { gameBySlug } from '../data/gameRegistry.js';
import { track } from '../core/analytics.js';
import { createGameSdk } from '../core/gameSdk.js';
import { playerStore } from '../core/storage.js';
const game=gameBySlug(document.body.dataset.game),root=document.querySelector('#game-detail');
if(!game){root.innerHTML='<p class="empty-state">게임을 찾을 수 없습니다.</p>';}else{let instance,score=0;const sdk=createGameSdk({game,onEvent:track});document.title=game.seoTitle;root.innerHTML=`<a class="back-link" href="/">← 모든 게임</a><section class="detail-hero"><div><p class="eyebrow">${game.category} · ${game.averagePlayTime}</p><h1>${game.title}</h1><p>${game.description}</p></div></section><section class="game-stage"><div class="game-toolbar"><div><span>Score</span><strong id="score">0</strong></div><div><span>Best</span><strong id="best">${playerStore.bestScore(game.id)}</strong></div><div class="game-actions"><button class="play-button" data-start>Play</button><button class="secondary-button" data-restart>Restart</button></div></div><div id="game-host" class="game-host"></div></section><aside class="safe-ad-zone detail-ad"><span>SAFE AD ZONE</span><p>게임 조작 영역에서 분리된 광고 영역</p></aside><section class="game-information"><div><h2>How to play</h2><p>${game.shortDescription}</p></div><div><h2>Controls</h2><p><b>Desktop</b> ${game.desktopControls.join(' · ')}<br><b>Mobile</b> ${game.mobileControls.join(' · ')}</p></div><div><h2>Difficulty</h2><p>${game.difficulty}</p></div></section>`;const load=async()=>{instance?.destroy();const host=document.querySelector('#game-host');if(!['orbit-tap','number-fold','merge-garden','lane-dodge'].includes(game.slug)){host.innerHTML='<p class="prototype-note">이 대표 게임은 다음 구현 순서에 있습니다.</p>';return;}const files={'orbit-tap':'orbitTap','number-fold':'numberFold','merge-garden':'mergeGarden','lane-dodge':'laneDodge'};const {mountGame}=await import(`../games/${files[game.slug]}.js`);instance=mountGame({container:host,sdk,game,onScore:(value)=>{score=value;document.querySelector('#score').textContent=value;document.querySelector('#best').textContent=sdk.setScore(value);}});};document.querySelector('[data-start]').onclick=async()=>{await load();instance?.start();};document.querySelector('[data-restart]').onclick=async()=>{await load();instance?.restart();};track('game_view',{game_id:game.id});window.addEventListener('beforeunload',()=>{instance?.destroy();sdk.destroy();},{once:true});}
